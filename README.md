# AI Stack Automator 🤖

Этот репозиторий содержит инструменты для автоматического развертывания локального AI-стека. С помощью одной команды вы подготовите окружение для работы с MCP (Model Context Protocol) серверами, используя **Ollama** и модель **Qwen Code (qwen:code)**.

## 🚀 Быстрый старт

Просто запустите следующую команду в корне проекта:

```bash
npm run setup:ai

```

## 🛠 Что делает скрипт?

Скрипт автоматически определяет вашу операционную систему (Windows, macOS или Linux) и выполняет следующие шаги:

1. **Проверка ресурсов**: Убеждается, что у вас есть минимум **8 ГБ RAM** и **10 ГБ** свободного места на диске.
2. **Установка Ollama**: Если Ollama не найдена в системе, скрипт скачает и установит актуальную версию.
3. **Запуск сервиса**: Инициализирует фоновый процесс Ollama и проверяет его доступность по адресу `http://localhost:11434`.
4. **Загрузка модели**: Скачивает модель `qwen:code` (Qwen Code model).
5. **Логирование**: Все действия и ошибки записываются в файл `.mcp/setup-ai.log`.

## 📋 Требования

* **Node.js**: версии 16 или выше.
* **ОС**: Windows 10/11, macOS или современный дистрибутив Linux.
* **Интернет**: Стабильное соединение для загрузки модели (~4.7 ГБ).

## 📂 Структура проекта

* `scripts/setup-ai.js` — Основной контроллер на Node.js.
* `scripts/setup-ai.sh` — Скрипт автоматизации для Unix-систем.
* `scripts/setup-ai.ps1` — Скрипт автоматизации для Windows (PowerShell).
* `.mcp/` — Директория с логами установки.

## 🔄 Повторный запуск

Скрипт является **идемпотентным**: если вы запустите его снова, он проверит наличие установленного ПО и пропустит уже выполненные шаги, не скачивая файлы заново.
